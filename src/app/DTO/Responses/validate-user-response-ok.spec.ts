import { ValidateUserResponseOK } from './validate-user-response-ok';

describe('ValidateUserResponseOK', () => {
  it('should create an instance', () => {
    expect(new ValidateUserResponseOK()).toBeTruthy();
  });
});
