import { RemoveMarkerResponseOK } from './remove-marker-response-ok';

describe('RemoveMarkerResponseOK', () => {
  it('should create an instance', () => {
    expect(new RemoveMarkerResponseOK()).toBeTruthy();
  });
});
