import { WsDrawingResponse } from './ws-drawing-response';

describe('WsDrawingResponse', () => {
  it('should create an instance', () => {
    expect(new WsDrawingResponse()).toBeTruthy();
  });
});
