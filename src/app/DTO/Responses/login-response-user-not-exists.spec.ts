import { LoginResponseUserNotExists } from './login-response-user-not-exists';

describe('LoginResponseUserNotExists', () => {
  it('should create an instance', () => {
    expect(new LoginResponseUserNotExists()).toBeTruthy();
  });
});
