import { UpdateMarkerColorResponseOK } from './update-marker-color-response-ok';

describe('UpdateMarkerColorResponseOK', () => {
  it('should create an instance', () => {
    expect(new UpdateMarkerColorResponseOK()).toBeTruthy();
  });
});
