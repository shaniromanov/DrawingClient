import { UploadImageResponseOK } from './upload-image-response-ok';

describe('UploadImageResponseOK', () => {
  it('should create an instance', () => {
    expect(new UploadImageResponseOK()).toBeTruthy();
  });
});
