import { CreateMarkerResponseOK } from './create-marker-response-ok';

describe('CreateMarkerResponseOK', () => {
  it('should create an instance', () => {
    expect(new CreateMarkerResponseOK()).toBeTruthy();
  });
});
