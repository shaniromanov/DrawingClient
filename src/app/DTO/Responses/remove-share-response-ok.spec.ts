import { RemoveShareResponseOK } from './remove-share-response-ok';

describe('RemoveShareResponseOK', () => {
  it('should create an instance', () => {
    expect(new RemoveShareResponseOK()).toBeTruthy();
  });
});
