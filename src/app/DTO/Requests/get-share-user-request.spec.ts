import { GetShareUserRequest } from './get-share-user-request';

describe('GetShareUserRequest', () => {
  it('should create an instance', () => {
    expect(new GetShareUserRequest()).toBeTruthy();
  });
});
