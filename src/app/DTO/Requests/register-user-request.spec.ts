import { RegisterUserRequest } from './register-user-request';

describe('RegisterUserRequest', () => {
  it('should create an instance', () => {
    expect(new RegisterUserRequest()).toBeTruthy();
  });
});
