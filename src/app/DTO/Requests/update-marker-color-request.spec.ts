import { UpdateMarkerColorRequest } from './update-marker-color-request';

describe('UpdateMarkerColorRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateMarkerColorRequest()).toBeTruthy();
  });
});
