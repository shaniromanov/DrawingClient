import { FreeDrawingRequest } from './free-drawing-request';

describe('FreeDrawingRequest', () => {
  it('should create an instance', () => {
    expect(new FreeDrawingRequest()).toBeTruthy();
  });
});
