import { ShareDocumentRequest } from './share-document-request';

describe('ShareDocumentRequest', () => {
  it('should create an instance', () => {
    expect(new ShareDocumentRequest()).toBeTruthy();
  });
});
