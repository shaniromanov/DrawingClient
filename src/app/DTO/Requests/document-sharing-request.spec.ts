import { DocumentSharingRequest } from './document-sharing-request';

describe('DocumentSharingRequest', () => {
  it('should create an instance', () => {
    expect(new DocumentSharingRequest()).toBeTruthy();
  });
});
